Act as a Principal Cyber Security Architect and Senior Full-Stack Developer. I need a comprehensive, production-ready blueprint, architecture layout, and core implementation code for an **Automated Web Penetration Testing Platform**. 

The application must allow a user to input a target URL via a web interface, trigger an asynchronous (background) security scan executing scenarios based on the **OWASP Top 10**, and automatically generate a downloadable PDF report detailing the vulnerabilities found.

Please break down the solution into the following structured sections:

### 1. System Architecture & Tech Stack
* **Frontend UI:** A clean, modern dashboard (e.g., Angular/Next.js) featuring a simple URL input bar, a scan initiation button, and a real-time progress/status tracker (Pending, Scanning, Generating Report, Completed).
* **Backend API:** A modular API (e.g., .NET Core / Python FastAPI / Node.js) handling scan requests, input validation, and report fetching.
* **Background Worker & Task Queue:** A robust asynchronous task queue worker (e.g., Celery with Redis, or Hangfire) to handle the long-running pentest execution in the background without blocking the main thread.
* **Security Engine Integration:** A wrapper/orchestrator mechanism that interacts with automated scanning tools or custom scripts (e.g., OWASP ZAP API, Nikto, or custom Axios/Fetch payloads) to execute OWASP Top 10 automated test suites.

### 2. Core Operational Workflow
Explain and outline the data flow for the following pipeline:
1.  **Input & Sanitization:** Client submits `target_url` -> Server validates and sanitizes input (preventing SSRF or command injection).
2.  **Job Enqueueing:** Server pushes a new pentest job to the background queue and returns a `job_id` immediately to the frontend.
3.  **Background Execution:** The worker picks up the job and fires automated test scenarios mapped to the OWASP Top 10 (Broken Object Level Authorization, SSRF, Injection, Cross-Site Scripting, etc.).
4.  **Vulnerability Compilation:** Raw scanner outputs are parsed, aggregated, and ranked by severity (Critical, High, Medium, Low, Info).
5.  **Automated PDF Generation:** A dedicated service transforms the compiled findings into a clean, professional PDF security audit document.

### 3. Database Schema (Design & Relationships)
Provide a database structure (SQL or NoSQL notation) tracking:
* `Scans` table (ScanID, TargetURL, Status, StartTime, EndTime, TriggeredBy)
* `Vulnerabilities` table (VulnerabilityID, ScanID, OWASP_Category, Name, Description, Severity, Evidence/Payload, Remediation)

### 4. Code Implementation (Core Modules)
Please provide clean, production-grade, modular code examples for:
* **A. The Background Worker Task:** The core function that kicks off the URL scan asynchronously.
* **B. The OWASP Top 10 Scanner Orchestrator:** A mock or real implementation showing how the engine sends payloads or triggers a tool (like invoking an OWASP ZAP active scan via API).
* **C. The PDF Export Service:** The backend logic that reads the vulnerability results from the database and compiles them into a PDF binary/file output using a standard library.

### 5. Essential Security & Guardrails
* How can we restrict the application to ensure users only scan URLs they own or have explicit authorization to test (e.g., verification tokens, TXT record checks, or strict subnet restrictions)?
* How do we prevent the scanner infrastructure from being blacklisted or abused?