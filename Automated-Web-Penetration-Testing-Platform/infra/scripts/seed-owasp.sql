-- Seed data placeholder. The OWASP Top 10 catalog is currently hard-coded in
-- packages/scan-engine. Move it here if/when a `owasp_categories` lookup
-- table is added in a future migration.
SELECT 1;
